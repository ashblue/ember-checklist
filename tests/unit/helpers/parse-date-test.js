import { parseDate } from '../../../helpers/parse-date';
import { module, test } from 'qunit';

module('Unit | Helper | parse date');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = parseDate(42);
  assert.ok(result);
});
