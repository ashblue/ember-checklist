import { everyNth } from '../../../helpers/every-nth';
import { module, test } from 'qunit';

module('Unit | Helper | every nth');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = everyNth(42);
  assert.ok(result);
});
